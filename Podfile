# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'HandAssure' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for HandAssure
  pod 'IQKeyboardManagerSwift'
  pod 'SwiftMessages'
  pod 'MBProgressHUD'
  pod 'SideMenu'
  pod 'Charts'
  pod 'Alamofire', '~> 5.2'

  target 'HandAssureTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'HandAssureUITests' do
    # Pods for testing
  end

end
